<%= form_tag search_action_url, :method => :get, :class => 'search ' do %>
    <%= render_hash_as_hidden_fields(params_for_search().except(:q, :search_field, :qt, :page, :utf8)) %>
    <div class="input-group input-group-lg">
      <% unless search_fields.empty? %>
          <label for="search_field" class="sr-only">
            <%= t('blacklight.search.form.search_field.label') %>
          </label>

          <span class="sr-only">
                       <%= t('blacklight.search.form.search_field.post_label') %>
                     </span>
      <% end %>
      <span class="input-group-btn hidden-xs">
                            <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => t('blacklight.search.form.search_field.title'), :class=>"input-lg") %>

                     </span>

      <label for="q" class="sr-only"><%= t('blacklight.search.form.search.label') %></label>
      <%= text_field_tag :q, params[:q], :placeholder => t('blacklight.search.form.search.placeholder'),
                         :class => "search_q q form-control", :id => "q", :autofocus => should_autofocus_on_search_box? %>

      <span class="input-group-btn ">
                <button type="submit" class="btn btn-primary btn-lg" id="search">
                  <span class="submit-search-text"><%=t('blacklight.search.form.submit')%></span>
                  <span class="glyphicon glyphicon-search"></span>
                </button>
      </span>
      <span class="input-group-btn">
          <span class="search-tooltip ">
            <span class="question-mark">?</span>
            <span class="tooltiptext">
                <div class="panel panel-default">
  <div class="panel-heading">Søgetips</div>
  <div class="panel-body">
    <ul class="advanced-help">

     <li>Kombiner flere søgetermer for at finde breve</li>

     <li>Brug ” for at søge efter en sætning</li>

      <li>Brug ”+” før et ord, hvis resultatet skal indeholde dette ord</li>

     <li>Brug ”–” for at udelukke ord fra søgningen</li>

      <li>Brug ”OR, ”AND” og ”NOT” til avancerede søgninger</li>

     <li>Bemærk, at systemet ikke understøtter wildcards</li>

      <li>Trunkering udføres automatisk af systemet</li>
   </ul>
  </div>
</div>
              </span>
          </span>
</span>
    </div>
<% end %>


